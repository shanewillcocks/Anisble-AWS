---
- hosts: all
  environment:
    AWS_ACCESS_KEY_ID: "{{ lookup('env','AWS_ACCESS_KEY_ID') }}"
    AWS_SECRET_ACCESS_KEY: "{{ lookup('env','AWS_SECRET_ACCESS_KEY') }}"
    # Set the proxy if needed
    http_proxy: "http://proxy:10568/"
    https_proxy: "http://proxy:10568/"
  become: false
  gather_facts: false
  roles:
    - role-pipeline_ami_transporter
